<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="HoYoShadeHub.Features.ViewHost.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lang="using:HoYoShadeHub.Language"
             xmlns:local="using:HoYoShadeHub.Features.ViewHost"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sfb="using:HoYoShadeHub.Features.Background"
             xmlns:sfg="using:HoYoShadeHub.Features.GameSelector"
             x:DefaultBindMode="OneWay"
             mc:Ignorable="d">

    <Grid>

        <sfb:AppBackground x:Name="AppBackground" CurrentGameId="{x:Bind CurrentGameId}" />


        <Border Width="56"
                HorizontalAlignment="Left"
                Background="{ThemeResource CustomAcrylicBrush}" />


        <Border Name="Border_OverlayMask"
                Background="{ThemeResource CustomAcrylicBrush}"
                Opacity="0">
            <Border.OpacityTransition>
                <ScalarTransition />
            </Border.OpacityTransition>
        </Border>


        <NavigationView Name="MainView_NavigationView"
                        CompactPaneLength="56"
                        IsBackButtonVisible="Collapsed"
                        IsPaneToggleButtonVisible="False"
                        ItemInvoked="NavigationView_ItemInvoked"
                        PaneDisplayMode="LeftCompact">
            <NavigationView.Resources>
                <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
                <Thickness x:Key="NavigationViewContentGridBorderThickness">0</Thickness>
                <!--<Thickness x:Key="NavigationViewContentMargin">0,48,0,0</Thickness>-->
            </NavigationView.Resources>

            <NavigationView.PaneCustomContent>
                <Border Height="52" />
            </NavigationView.PaneCustomContent>

            <NavigationView.MenuItems>
                <NavigationViewItemSeparator />
                <!--  启动器  -->
                <NavigationViewItem Name="NavigationViewItem_Launcher"
                                    IsSelected="True"
                                    Tag="GameLauncherPage"
                                    ToolTipService.ToolTip="{x:Bind lang:Lang.MainPage_Launcer}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE80F;" />
                    </NavigationViewItem.Icon>
                    <NavigationViewItem.Content>
                        <Border Height="40">
                            <TextBlock VerticalAlignment="Center" Text="{x:Bind lang:Lang.MainPage_Launcer}" />
                        </Border>
                    </NavigationViewItem.Content>
                </NavigationViewItem>
                <!--  游戏设置  -->
                <NavigationViewItem Name="NavigationViewItem_GameSetting"
                                    Tag="GameSettingPage"
                                    ToolTipService.ToolTip="{x:Bind lang:Lang.LauncherPage_GameSetting}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE7FC;" />
                    </NavigationViewItem.Icon>
                    <NavigationViewItem.Content>
                        <Border Height="40">
                            <TextBlock VerticalAlignment="Center" Text="{x:Bind lang:Lang.LauncherPage_GameSetting}" />
                        </Border>
                    </NavigationViewItem.Content>
                </NavigationViewItem>
                <!--  游戏截图  -->
                <NavigationViewItem Name="NavigationViewItem_Screenshot"
                                    Tag="ScreenshotPage"
                                    ToolTipService.ToolTip="{x:Bind lang:Lang.MainPage_GameScreenshot}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE91B;" />
                    </NavigationViewItem.Icon>
                    <NavigationViewItem.Content>
                        <Border Height="40">
                            <TextBlock VerticalAlignment="Center" Text="{x:Bind lang:Lang.MainPage_GameScreenshot}" />
                        </Border>
                    </NavigationViewItem.Content>
                </NavigationViewItem>
                <!--  插件：按游戏分开（插件文件全局一份，开关写各游戏自己的 ReShade.ini）  -->
                <NavigationViewItem Name="NavigationViewItem_Plugins"
                                    Tag="GamePluginPage"
                                    ToolTipService.ToolTip="插件">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEA86;" />
                    </NavigationViewItem.Icon>
                    <NavigationViewItem.Content>
                        <Border Height="40">
                            <TextBlock VerticalAlignment="Center" Text="插件" />
                        </Border>
                    </NavigationViewItem.Content>
                </NavigationViewItem>
                <!--  DLL 配置：插件要的那套运行时（nvngx_dlssnr.dll / sl.*.dll）都装在插件目录里  -->
                <NavigationViewItem Name="NavigationViewItem_DllConfig"
                                    Tag="DllConfigPage"
                                    ToolTipService.ToolTip="DLL 配置">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE74C;" />
                    </NavigationViewItem.Icon>
                    <NavigationViewItem.Content>
                        <Border Height="40">
                            <TextBlock VerticalAlignment="Center" Text="DLL 配置" />
                        </Border>
                    </NavigationViewItem.Content>
                </NavigationViewItem>
                <!--  模型替换（XXMI）：MI 实例 + Mods 管理  -->
                <NavigationViewItem Name="NavigationViewItem_Xxmi"
                                    Tag="XxmiPage"
                                    ToolTipService.ToolTip="模型替换（XXMI）">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE7B8;" />
                    </NavigationViewItem.Icon>
                    <NavigationViewItem.Content>
                        <Border Height="40">
                            <TextBlock x:Name="TextBlock_XxmiNav" VerticalAlignment="Center" Text="模型替换" />
                        </Border>
                    </NavigationViewItem.Content>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <!--  左下角（设置上方）：全局视角 —— 装/删扩展包 + 重命名式全局开关  -->
            <NavigationView.FooterMenuItems>
                <NavigationViewItem Name="NavigationViewItem_GlobalPlugins"
                                    Tag="GlobalPluginPage"
                                    ToolTipService.ToolTip="全局插件">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8B7;" />
                    </NavigationViewItem.Icon>
                    <NavigationViewItem.Content>
                        <Border Height="40">
                            <TextBlock VerticalAlignment="Center" Text="全局插件" />
                        </Border>
                    </NavigationViewItem.Content>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <Frame Name="MainView_Frame" Margin="0,48,0,0" />

        </NavigationView>



        <sfg:GameSelector x:Name="GameSelector" CurrentGameChanged="GameSelector_CurrentGameChanged" />


    </Grid>

</UserControl>
